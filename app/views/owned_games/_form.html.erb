<%= form_with(model: owned_game, local: true) do |form| %>
  <% if owned_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owned_game.errors.count, "error") %> prohibited this owned_game from being saved:</h2>

      <ul>
        <% owned_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form_with(model: owned_game, local: true) do |f| %>
      <%= label_tag(:condition, "Condition:") %>
      <%= select_tag(:condition, options_for_select([['New', 0], ['Like New', 1], ['Used', 2]])) %>    
      <%= label_tag(:trade_status, "Trade Status:") %>
      <%= select_tag(:trade_status, options_for_select([['Tradeable', 0], ['Not For Trade', 1]])) %>
      <%= label_tag(:price, "Price:") %>
      <%= number_field(:price, :price, in: 0.0..2000.0, step: 0.01) %>
      <%= f.submit %>
    <% end %>
  </div>
<% end %>