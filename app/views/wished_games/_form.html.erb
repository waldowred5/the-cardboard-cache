<%= form_with(model: wished_game, local: true) do |form| %>
  <% if wished_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wished_game.errors.count, "error") %> prohibited this wished_game from being saved:</h2>

      <ul>
        <% wished_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
